(()=>{'use strict';const RATE=.35,PRICE=590;const area=document.querySelector('#area');const amount=document.querySelector('#amount');const price=document.querySelector('#price');const modal=document.querySelector('#modal');const form=document.querySelector('#leadForm');const status=document.querySelector('#formStatus');const fmt=(n,d=0)=>Number(n).toLocaleString('ru-RU',{maximumFractionDigits:d});function calculate(){const value=parseFloat(String(area?.value||0).replace(',','.'));if(!Number.isFinite(value)||value<=0){amount.textContent='—';price.textContent='—';return}const kg=value*RATE;amount.textContent=`${fmt(kg,2)} кг`;price.textContent=`${fmt(kg*PRICE)} BYN`}function open(){modal.classList.add('open');modal.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'}function close(){modal.classList.remove('open');modal.setAttribute('aria-hidden','true');document.body.style.overflow=''}area?.addEventListener('input',calculate);calculate();document.querySelectorAll('[data-open]').forEach(el=>el.addEventListener('click',open));document.querySelectorAll('[data-close]').forEach(el=>el.addEventListener('click',close));document.addEventListener('keydown',e=>{if(e.key==='Escape')close()});form?.addEventListener('submit',async e=>{e.preventDefault();const data=new FormData(form);const text=`Заявка с сайта ВЕНИК, ВДГ\nИмя: ${data.get('name')||''}\nТелефон: ${data.get('phone')||''}\nКомментарий: ${data.get('message')||''}`;try{await navigator.clipboard.writeText(text);status.textContent='Заявка скопирована. Позвоните по номеру +375 29 656-52-52.'}catch{status.textContent='Позвоните по номеру +375 29 656-52-52.'}})})();